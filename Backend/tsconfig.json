{
  "compilerOptions": {
    /* Basic “ESNext” output for node 16+ */
    "target": "ES2020",
    "module": "commonjs",

    /* Where to find your source, and where to put the compiled output */
    "rootDir": "./src",
    "outDir": "./dist",

    /* Allow importing JSON, enable __dirname/__filename, etc. */
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    /* If you genuinely need .tsx (you can also rename index.tsx → index.ts) */
    "jsx": "preserve",

    /* Strictness options */
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
